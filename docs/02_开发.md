# 02 开发

## 环境与安装

在仓库根目录（例如 `/path/to/SpeedVQA`）执行：

```bash
pip install -r requirements.txt
pip install -e .
```

可编辑安装便于修改 `speedvqa/` 后立即生效。

## 目录与入口

- **包代码**：`speedvqa/`（`data`、`models`、`engine`、`inference`、`export` 等）。
- **一键 CLI**：`python -m speedvqa.cli.onekey_train`、`onekey_predict`、`onekey_export`；**`pip install -e .`** 后为 **`speedvqa-train`**、**`speedvqa-predict`**、**`speedvqa-export`**（见 **`pyproject.toml`** 的 **`[project.scripts]`**）。
- **Shell 包装**：仓库根 `scripts/onekey_*.sh`；清理可用 **`scripts/onekey_clean.sh`**（`runs/`、缓存等）。

公开 API 以 `speedvqa/__init__.py` 的 `__all__` 为准。

### 推荐导入（与顶层 API 的分工）

| 用途 | 推荐方式 |
|------|----------|
| 数据集、标注校验、加载 YAML、从配置构建模型 | `from speedvqa import ...` 或显式 `speedvqa.data` / `speedvqa.utils.config` / `speedvqa.models` |
| 训练与优化器、调度、超参搜索 | `from speedvqa.engine import train, ConfigurableTrainer` 等（见 `speedvqa/engine/__init__.py`） |
| 多格式导出 | `speedvqa.export`（说明见 `speedvqa/export/README.md`） |
| ROI 推理（PyTorch / ONNX / TensorRT） | `speedvqa.inference`（说明见 `speedvqa/inference/README.md`） |
| 命令行一键流程 | `python -m speedvqa.cli.onekey_train`（及 `onekey_predict`、`onekey_export`），或 **`speedvqa-train`** 等 |

安装 **`pip install`** 生成的 wheel 时，**不包含** `speedvqa.tests` 与 `speedvqa.examples`（见根目录 **`pyproject.toml`** 的 `exclude`）；克隆源码仓库开发时二者仍完整可用。

## 测试

从仓库根：

```bash
pytest
```

或：

```bash
python speedvqa/tests/run_tests.py
```

配置见根目录 `pyproject.toml`（`testpaths`）与 `speedvqa/pytest.ini`。Hypothesis、`collect_ignore` 等见 `speedvqa/tests/conftest.py`。

### 测试与质量

- **`speedvqa/tests/`**：从仓库根运行 **`pytest`** 时由根目录 **`pyproject.toml`** 的 `testpaths` 指向此处；在 **`speedvqa/`** 下可执行 **`pytest -c pytest.ini`**。
- **`speedvqa/tests/conftest.py`**：`collect_ignore` 排除 `test_model_basic.py`、`test_model_factory.py`，避免与「独立脚本式」运行重复；另配置 Hypothesis。
- 建议在目标环境中运行与 GPU、TensorRT 相关的测试前，确认驱动与库版本与 `requirements.txt` 一致。

## 延伸阅读

- 项目定位、目录树、典型工作流与扩展新骨干步骤：**[01_设计.md](./01_设计.md)**
